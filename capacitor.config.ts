import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.burakakkaya.tavla',
  appName: 'Backgammon Limitless',
  webDir: 'dist'
};

export default config;
